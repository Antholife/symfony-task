{% extends 'base.html.twig' %}

{% block title %}Edit Task{% endblock %}

{% block body %}
    <div class="container">
        <div class="form new">
            <h1>Edit Task</h1>

            {{ include('task/_form.html.twig', {'button_label': 'Update'}) }}

            <button class="btn secondary" type="button" onclick="window.location.href='{{ path('app_task_index') }}'">
                Back to list
            </button>
        </div>
    </div>
{% endblock %}
